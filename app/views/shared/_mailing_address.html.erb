<% if user.is_a? PersonUser %>
  <%= user.email %>
<% else %>
  <%= user.primary_address %>
  <%= raw("<br/>") + "#{user.city}" if user.city.present? %>
  <%= raw("<br/>") + "#{user.state}" if user.state.present? %>
  <%= raw("<br/>") + "#{user.zipcode}" if user.zipcode.present? %>
<% end %>