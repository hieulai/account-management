<%= form_for @user, :as => :user, url: user_path, :html => {:class => 'form-horizontal'} do |f| %>
  <%= render partial: 'shared/errors', locals: {object: @user} %>

  <%= f.fields_for :people, @user.profile do |p| %>
    <%= render :partial => "shared/person/person_user_form", :locals => {:p => p} %>
  <% end %>

  <div class="control-group">
      <div class="controls">
          <%= f.submit "Save", :class => 'btn' %>
          <%= link_to "Cancel", current_users_path, :class => 'btn' %>
      </div>
  </div>
<% end %>